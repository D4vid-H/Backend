{{#if hasAny}}
    <tr>
        <td class="">{{title}}</td>
        <td class=""><img class="image" alt="item img" src="{{thumbnail}}" /></td>
        <td class="">${{price}}</td>
    </tr>
    {{else}}
    <div>
        <h2>{{message}}</h2>
    </div>
{{/if}}
