
<h1>Productos</h1>
{{#if hasAny}}
    {{#each productos}}
        <li>
        <h2>{{this.title}}</h2>
        <img src="{{this.thumbnail}}" alt="">
        <p>{{this.price}}</p>
        </li>
    {{/each}}
  {{else}}
    <h2>No hay productos</h2>
{{/if }}
